$(function () {
    chartData();
});

const chartData = () => {
    window.ZD_CHART_DATA = window.ZD_CHART_DATA || {};

    // 데이터는 report.html의 인라인 스크립트에서 설정됨
    // window.ZD_CHART_DATA는 report.html에서 설정됩니다
}